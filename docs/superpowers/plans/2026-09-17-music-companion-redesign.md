# 音乐 AI 伴侣重设计实现计划

1. 为 Flask 首页、搜索参数和流式前端状态补回归测试，先复现旧入口与异常状态。
2. 让 Flask 提供 `web/dist`，开发环境仍使用 Vite；移除旧模板入口依赖。
3. 重写 React 页面结构与样式，只保留播放器、伴侣对话和三个快捷提示。
4. 加固 SSE：超时、错误解析、异常结束、重复提交保护。
5. 加固音乐搜索参数并检查现有播放相关接口。
6. 构建前端，运行 Python/Node 测试；启动 Demo 服务做浏览器交互和响应式检查。
7. 根据真实截图修正视觉细节，最后复跑完整验证。
